create table beanevent (eventid int GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1),contect varchar(200),reminderTime timestamp,state varchar(2), remindermode varchar(2),classification varchar(2),userid varchar(30),PRIMARY KEY(eventid));


create table beanuser (userid varchar(30) primary key,pwd varchar(30),createdate time);